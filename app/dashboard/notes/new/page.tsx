import CreateNote from "@/src/features/note/components/createNote";

const Page = () => {
  return (
    <section className="w-full h-full">
      <CreateNote />
    </section>
  );
};

export default Page;
