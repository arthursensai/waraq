# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.setup.ts >> authenticate
- Location: tests/e2e/auth.setup.ts:10:6

# Error details

```
Error: expect(page).not.toHaveURL(expected) failed

Expected pattern: not /\/auth\/login/
Received string: "http://localhost:3100/auth/login"
Timeout: 15000ms

Call log:
  - Expect "not toHaveURL" with timeout 15000ms
    34 × locator resolved to <html lang="en" class="cairo_a5286c6f-module__jWYTJG__variable light">…</html>
       - unexpected value "http://localhost:3100/auth/login"

```

```yaml
- main:
  - text: Login Enter your email and password below to login to your account.
  - group:
    - text: "Email:"
    - textbox "m@example.com": mohammedaitsidibah@gmail.com
  - group:
    - text: "Password:"
    - textbox: 123789mohamed
  - paragraph: Invalid login credentials
  - button "Login"
  - link "Forgot your password?":
    - /url: /auth/forgot-password
  - text: Don't have an account?
  - link "Sign up":
    - /url: /auth/sign-up
- alert
```

# Test source

```ts
  1  | import { test as setup, expect } from "@playwright/test";
  2  | import fs from "node:fs";
  3  | import path from "node:path";
  4  | 
  5  | const authFile = path.join(__dirname, ".auth/user.json");
  6  | 
  7  | const TEST_EMAIL = process.env.E2E_TEST_USER_EMAIL;
  8  | const TEST_PASSWORD = process.env.E2E_TEST_USER_PASSWORD;
  9  | 
  10 | setup("authenticate", async ({ page }) => {
  11 |   fs.mkdirSync(path.dirname(authFile), { recursive: true });
  12 | 
  13 |   if (!TEST_EMAIL || !TEST_PASSWORD) {
  14 |     // No dedicated test-user credentials configured (e.g. a contributor's
  15 |     // machine, or a CI run that intentionally only checks the
  16 |     // unauthenticated specs). Write an empty, logged-out storage state so
  17 |     // the "chromium" project can still start - authenticated specs detect
  18 |     // the missing env vars themselves and skip.
  19 |     fs.writeFileSync(authFile, JSON.stringify({ cookies: [], origins: [] }));
  20 |     return;
  21 |   }
  22 | 
  23 |   await page.goto("/auth/login");
  24 |   await page.getByPlaceholder("m@example.com").fill(TEST_EMAIL);
  25 |   await page.locator('input[name="password"]').fill(TEST_PASSWORD);
  26 |   await page.getByRole("button", { name: /login/i }).click();
  27 | 
  28 |   // A successful login redirects away from /auth/login, either to
  29 |   // /dashboard or /onboarding for a brand-new test user.
> 30 |   await expect(page).not.toHaveURL(/\/auth\/login/, { timeout: 15_000 });
     |                          ^ Error: expect(page).not.toHaveURL(expected) failed
  31 | 
  32 |   await page.context().storageState({ path: authFile });
  33 | });
  34 | 
```